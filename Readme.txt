Para instalar, clone la carpeta con git pull origin master --allow-unrelated-histories

Verifique que la carpeta src contenga todos los archivos y si no descarguelo o creelos en su computador.

Modifique el archivo pom.xml de acuerdo con la versión de Java que disponga, si es necesario,
genere uno nuevo en https://start.spring.io
tenga en cuenta que se deben instalar las siguientes dependencias:
-H2
-Spring web
-Spring Data JPA